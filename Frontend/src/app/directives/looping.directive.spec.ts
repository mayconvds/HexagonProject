import { LoopingDirective } from './looping.directive';

describe('LoopingDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopingDirective();
    expect(directive).toBeTruthy();
  });
});
